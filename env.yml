name: SC203

channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults

dependencies:
  # ------------------------------------------------------------
  # Core
  # ------------------------------------------------------------
  - python=3.10
  - pip

  # ------------------------------------------------------------
  # PyTorch (CUDA 11.8) – install via conda (STABLE)
  # ------------------------------------------------------------
  - pytorch=2.1.0
  - torchvision=0.16.0
  - torchaudio=2.1.0
  - pytorch-cuda=11.8

  # ------------------------------------------------------------
  # Pip dependencies
  # ------------------------------------------------------------
  - pip:
      # Core scientific stack
      - "numpy<2.0.0"
      - scipy
      - h5py
      - pyyaml

      # Vision & visualization
      - opencv-python
      - matplotlib
      - supervision
      - pycocotools

      # NLP / Transformers
      - transformers
      - accelerate
      - huggingface-hub

      # Utils / formatting
      - addict
      - yapf
      - timm
